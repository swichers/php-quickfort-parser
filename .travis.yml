language: php

php:
    - '7.1'
    - '7.2'
    - '7.3'

git:
    quiet: true
    depth: false

branches:
    only:
        - master
        - develop

install:
    - composer install --no-interaction
